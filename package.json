{
  "name": "@elvatis/openclaw-gpu-bridge",
  "version": "0.1.0",
  "description": "OpenClaw plugin — Remote GPU compute via FastAPI microservice",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest"
  },
  "keywords": ["openclaw", "openclaw-plugin", "gpu", "bert-score", "ml", "remote-compute"],
  "author": "Elvatis <hello@elvatis.com>",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
